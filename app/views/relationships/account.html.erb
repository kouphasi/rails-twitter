<h1><%= @user.name %></h1>
<div>
   <% if current_user != @user %>
   <% if current_user.following?(@user) %>
   <%= link_to 'フォロー外す', account_table_relationships_path(@user.id), method: :delete %>
   <% else %>
   <%= link_to 'フォローする', userpage_follow_path(account_table_id:@user.id), method: :post %>
   <% end %>
   <% end %>
</div>
